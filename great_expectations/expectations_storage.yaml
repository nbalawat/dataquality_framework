stores:
  validations_store:
    class_name: ValidationsStore
    store_backend:
      class_name: TupleGCSStoreBackend
      bucket: your_gcs_bucket  # Replace with your GCS bucket
      prefix: "validations/"
